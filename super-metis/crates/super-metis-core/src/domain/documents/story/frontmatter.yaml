id: {{ slug }}
level: story
title: "{{ title }}"
short_code: "{{ short_code }}"
created_at: {{ created_at }}
updated_at: {{ updated_at }}
parent_id: {{ parent_id }}
blocked_by:
{% for id in blocked_by %}  - "{{ id }}"
{% endfor %}
archived: {{ archived }}

tags:
{% for tag in tags %}  - "{{ tag }}"
{% endfor %}
exit_criteria_met: {{ exit_criteria_met }}
story_type: {{ story_type }}
risk_level: {{ risk_level }}
design_context_refs:
{% for ref in design_context_refs %}  - "{{ ref }}"
{% endfor %}
schema_version: 1
epic_id: {{ epic_id }}
