id: {{ slug }}
level: validation_record
title: "{{ title }}"
short_code: "{{ short_code }}"
created_at: {{ created_at }}
updated_at: {{ updated_at }}
archived: {{ archived }}

tags:
{% for tag in tags %}  - "{{ tag }}"
{% endfor %}

exit_criteria_met: {{ exit_criteria_met }}
schema_version: 1
epic_id: {{ epic_id }}
validation_type: "{{ validation_type }}"
result: "{{ result }}"
related_artifact: "{{ related_artifact }}"
required: {{ required }}
