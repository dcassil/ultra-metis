id: {{ slug }}
level: rules_config
title: "{{ title }}"
short_code: "{{ short_code }}"
created_at: {{ created_at }}
updated_at: {{ updated_at }}
archived: {{ archived }}

tags:
{% for tag in tags %}  - "{{ tag }}"
{% endfor %}

exit_criteria_met: {{ exit_criteria_met }}
schema_version: 1
epic_id: {{ epic_id }}

protection_level: "{{ protection_level }}"
scope: "{{ scope }}"
source_architecture_ref: {{ source_architecture_ref }}