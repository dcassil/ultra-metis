# {{ title }}

## Problem Description

<!-- What was detected, when, and what is the impact. Reference the validation or quality record that surfaced this. -->
- **Detected By**: (short code of the validation/quality record that found the problem)
- **Detection Date**: (when the problem was identified)
- **Severity**: (critical / high / medium / low)
- **Impact**: (what breaks or degrades if this is not fixed)

## Affected Scope

<!-- Be specific about what files, modules, components, or systems are impacted. -->
- **Components**: (list affected components or modules)
- **Files**: (list key files if known)
- **Dependencies**: (upstream/downstream systems affected)

## Required Fixes

<!-- Enumerate each fix as a concrete, actionable step. -->
1. (specific change required, with file paths or module names)
2. (next fix step)

## Verification Plan

<!-- How will we confirm the fix resolved the problem? What validation must pass? -->
- **Re-run Validation**: (short code of the validation to re-run)
- **Expected Outcome**: (what metrics/checks should now pass)
- **Sign-off Required**: (yes/no, and by whom)