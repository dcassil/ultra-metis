id: {{ slug }}
level: design_change_proposal
title: "{{ title }}"
short_code: "{{ short_code }}"
created_at: {{ created_at }}
updated_at: {{ updated_at }}
parent_id: {{ parent_id }}
archived: {{ archived }}
target_artifact: "{{ target_artifact }}"
proposal_status: "{{ proposal_status }}"
reviewer: {{ reviewer }}

tags:
{% for tag in tags %}  - "{{ tag }}"
{% endfor %}

exit_criteria_met: {{ exit_criteria_met }}
schema_version: 1
