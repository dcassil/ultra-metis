# {{ title }}

## Constraint Description

<!-- State the constraint as a clear, unambiguous rule. Use MUST/MUST NOT/SHOULD language. -->
- **Rule Statement**: (e.g., "All public API endpoints MUST have OpenAPI documentation")
- **Type**: (architectural / process / compliance / security / performance)
- **Source**: (where this constraint originates: ADR, policy, regulation, technical limitation)

## Scope

<!-- Define exactly what this constraint applies to. Be precise enough that automated tools could enforce it. -->
- **Applies To**: (repos, packages, components, file patterns, teams)
- **Does Not Apply To**: (explicit exclusions)

## Enforcement

<!-- How is this constraint currently enforced? List every enforcement mechanism. -->
- **Automated**: (linting rules, CI checks, pre-commit hooks -- list tool and rule name)
- **Manual**: (code review checklist items, approval gates)
- **RulesConfig Reference**: (short code of the RulesConfig that encodes this constraint, if any)

## Exceptions

<!-- Document any approved exceptions. Each exception should reference an ApprovalRecord. -->
| Exception | Scope | Approval | Expiry |
|-----------|-------|----------|--------|
| (description) | (what is exempted) | (approval record short code) | (date or "permanent") |